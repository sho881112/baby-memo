<h1>id: <%= @message.id %> のMEMO詳細ページ</h1>

<table class="table table-bordered">
  <tr>
    <th>id</th>
    <td><%= @message.id %></td>
  </tr>
  <tr>
    <th>日付</th>
    <td><%= @message.title %></td>
  </tr>
  <tr>
    <th>MEMO</th>
    <td><%= @message.content %></td>
  </tr>
</table>

<%= link_to 'このMEMOを編集する', edit_message_path(@message), class: 'btn btn-light' %>
<%= link_to 'このMEMOを削除する', @message, method: :delete, data: { confirm: '本当に削除してよろしいですか？' }, class: 'btn btn-danger' %>